---
name: Deploy & Track Setup
description: Wire deploy-engineer Open Cloud publish + kpi-tracker 30-day monitoring + learning-agent pattern capture — validate full post-build pipeline
assignee: deploy-engineer
---

Wire the post-build pipeline: deploy-engineer publishes via Open Cloud API → confirms live + joinable → community-manager posts Group shout → kpi-tracker starts 30-day monitoring window → learning-agent receives final report on day 30 → retrospective produced for CEO. Validate with first real game deploy. Confirm all secrets (ROBLOX_API_KEY, ROBLOX_UNIVERSE_ID) are configured in environment.
