---
name: growth-audit
description: Audit a Roblox game's discoverability — title/tag SEO, thumbnail CTR, algorithm signals, traffic sources — with specific optimization recommendations
---

Audit game discoverability. Check: title keyword placement + length, description first-2-sentence hook, tag relevance (5 tags), thumbnail CTR estimate, algorithm signals (2-min retention rate vs 40% threshold), traffic source breakdown (search vs recommended vs direct). Output: current score per dimension + specific rewrite recommendations. Implement approved changes via community-manager.
